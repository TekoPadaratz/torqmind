---
description: Regras de segurança multi-tenant do TorqMind — INEGOCIÁVEIS
globs:
  - "apps/api/**/*.py"
  - "apps/web/**/*.ts"
  - "apps/web/**/*.tsx"
  - "sql/**/*.sql"
alwaysApply: true
---

# 🔒 Segurança Multi-Tenant — Regras de Ouro

## ⛔ CRIME #1 — Vazamento entre Tenants

NENHUMA query, endpoint ou lógica pode existir sem aplicar os filtros de escopo:
- `id_empresa` → SEMPRE obrigatório
- `id_filial` → quando aplicável ao contexto

Exemplos corretos:
```sql
-- ✅ Correto
WHERE id_empresa = :id_empresa AND id_filial = :id_filial

-- ❌ CRIME — nunca faça isso
SELECT * FROM torqmind_mart_rt.sales_daily_rt;
```

```python
# ✅ Correto
.filter(Venda.id_empresa == tenant.id_empresa)

# ❌ CRIME
.all()  # sem filtro de tenant
```

## ⛔ CRIME #2 — Hot path BI lento / fonte errada

O Dashboard Geral e endpoints de overview **NUNCA** leem `dw.fact_*` nem usam `mart.*` PostgreSQL como resposta silenciosa da API de BI.

Leitura analítica canônica: ClickHouse (`torqmind_mart_rt` / `torqmind_mart`) via `repos_analytics` / `repos_mart_realtime`.
Exceções PG analíticas só se registradas em `apps/api/app/analytics_pg_exceptions.json`.
Contrato: `.cursor/rules/06-clickhouse-bi-reads.mdc`.

```python
# ✅ Correto — facade BI / mart realtime CH (com id_empresa)
# repos_analytics → REALTIME_FUNCTIONS / torqmind_mart_rt.*

# ❌ CRIME — fato no hot path da tela
SELECT * FROM dw.fact_venda WHERE id_empresa = :id_empresa
```

## ⛔ CRIME #3 — Assumer nomes de colunas

A fonte da verdade são as migrations SQL (`sql/migrations/MANIFEST.json` + arquivos).
Nunca assuma nomes. Sempre verifique.

Exemplo crítico: em `dw.fact_venda` o valor chama-se `total_venda` (singular), não `valor`, não `amount`.
Agregados/marts podem expor `total_vendas` (plural) — não misturar camadas.

## ⛔ CRIME #4 — Motor de Risco sem tratamento

O grão de `dw.fact_risco_evento` é por `id_comprovante`.
Eventos de "usuários outliers" DEVEM usar ID sintético negativo para evitar colisão na constraint `uq_fact_risco_evento_nk`.

## Hierarquia de Permissões

- Permissões de platform (backoffice interno) ≠ permissões de usuários do tenant
- Estado de empresa e filial influencia acesso real ao produto
- Nunca confunda os dois contextos de permissão
